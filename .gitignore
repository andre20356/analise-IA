node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Backups automáticos (nunca commitar)
backups/
*.bak.*

# Banco de dados local (nunca commitar — contém chaves criptografadas)
db.json
